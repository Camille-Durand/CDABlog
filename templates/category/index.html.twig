{% extends 'header.html.twig' %}

{% block title %}Ajouter une catégorie{% endblock %}

{% block body %}
    {{form_start(form)}}
    {{form_row(form.name)}}
    <input type="submit" value="Ajouter">
    {{form_end(form)}}
    <p>{{msg}}</p>
{% endblock %}
