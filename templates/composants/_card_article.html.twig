{% block card %}

<a href="{{ path('app_article', {id:article.id}) }}" class="flex flex-col items-center bg-white border border-gray-200 rounded-lg shadow md:flex-row md:max-w-xl hover:bg-gray-100 dark:border-gray-700 dark:bg-gray-800 dark:hover:bg-gray-700">
    <div class="md:flex">
        <div class="md:shrink-0">
            <!-- <img class="h-48 w-full object-cover md:h-full md:w-48" src={{article.imgArticle}} alt="Image de l'article"> -->
            <div class="flex flex-col justify-between p-4 leading-normal">
                <h5 class="mb-2 text-2xl font-bold tracking-tight text-gray-900 dark:text-white">{{article.title}}</h5>
                <p class="mb-3 font-normal text-gray-700 dark:text-gray-400">{{article.createAt | date('d/m/Y') }}</p>
            </div>
        </div>
    </div>
</a>

{% endblock %}