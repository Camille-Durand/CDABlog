<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="https://idkwhattodo.ddns.net/img/anubisrotation.gif">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        <header class="flex flex-wrap sm:justify-start sm:flex-nowrap z-50 w-full bg-gradient-to-br from-purple-600 to-blue-500 text-sm py-4 dark:bg-gray-800">
            <nav class="max-w-[85rem] w-full mx-auto px-4 sm:flex sm:items-center sm:justify-between" aria-label="Global">
                <a class="flex-none text-xl text-white font-semibold dark:text-white" href="/home">CDABlog</a>
                <img src="https://idkwhattodo.ddns.net/img/anubisrotation.gif" width="60" alt="Anubis" />
                <div class="flex flex-row items-center gap-5 px-8">
                    <a class="font-medium text-white hover:text-gray-400 dark:text-gray-400 dark:hover:text-gray-500 dark:focus:outline-none dark:focus:ring-1 dark:focus:ring-gray-600" href="/home" aria-current="page">Home</a>
                    <a class="font-medium text-white hover:text-gray-400 dark:text-gray-400 dark:hover:text-gray-500 dark:focus:outline-none dark:focus:ring-1 dark:focus:ring-gray-600" href="/category/add">Catégorie</a>
                    <a class="font-medium text-white hover:text-gray-400 dark:text-gray-400 dark:hover:text-gray-500 dark:focus:outline-none dark:focus:ring-1 dark:focus:ring-gray-600" href="/user/add">Utilisateur</a>
                </div>
            </nav>
          </header>
        {% block body %}{% endblock %}
    </body>
</html>
